# Copilot Instructions

## Project Guidelines
- The app is currently tested and working, so preserve existing logic during modernization; code improvements are acceptable when they clearly improve the solution; the user is open to upgrading to .NET 10.
- For the Avalonia migration, target a cross-platform app rather than Windows-only, and it is acceptable to replace current hashing libraries if a more suitable option is found.